import { memo } from 'react'
interface IProps {}

const _{{pascalCase name}} = (props: IProps) => {
  return <></>;
};

const {{pascalCase name}} = memo(_{{pascalCase name}})
export default {{pascalCase name}};
